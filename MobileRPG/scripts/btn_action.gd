#creamos un scene base para los botones

#lo hacemos para no estar conectando por cada boton, 
#los calculos de lo que hará cada boton se harán aqui dentro

extends Button

const BattleUnits = preload("res://Resources/BattleUnits.tres")

func _on_pressed():
	pass 
